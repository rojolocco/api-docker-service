name: Build and Push Docker Image to GHCR

on:
  push:
    branches: [main, master]
    tags: ["v*"]
  pull_request:
    branches: [main, master]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

permissions:
  contents: read
  packages: write

jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.13

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install uv
          uv pip install --system -e . pytest

      - name: Run tests
        run: |
          uv run pytest

  # build-and-push:
  #   needs: test
  #   runs-on: ubuntu-22.04

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
        
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
        
  #     - name: Extract version
  #       id: get-version
  #       run: |
  #         VERSION=$(grep -Po '(?<=^version = ")[^"]*' pyproject.toml | head -1)
  #         echo "version=$VERSION" >> $GITHUB_OUTPUT
  #         echo "Extracted version: $VERSION"

  #     - name: Log in to GitHub Container Registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ${{ env.REGISTRY }}
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Extract metadata
  #       id: meta
  #       uses: docker/metadata-action@v5
  #       with:
  #         images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
  #         tags: |
  #           type=ref,event=branch
  #           type=ref,event=pr
  #           type=semver,pattern={{version}}
  #           type=semver,pattern={{major}}.{{minor}}
  #           type=raw,value=latest,enable={{is_default_branch}}
  #           type=raw,value=${{ steps.get-version.outputs.version }}

  #     - name: Build and push Docker image
  #       uses: docker/build-push-action@v6
  #       with:
  #         context: .
  #         file: ./Dockerfile
  #         platforms: linux/amd64
  #         push: true
  #         tags: ${{ steps.meta.outputs.tags }}
  #         labels: ${{ steps.meta.outputs.labels }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  #     - name: Generate deployment summary
  #       run: |
  #         echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
  #         echo "- **Registry**: ${{ env.REGISTRY }}" >> $GITHUB_STEP_SUMMARY
  #         echo "- **Image**: ${{ env.IMAGE_NAME }}" >> $GITHUB_STEP_SUMMARY
  #         echo "- **Tags**: ${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
  #         echo "" >> $GITHUB_STEP_SUMMARY
  #         echo "### Pull command:" >> $GITHUB_STEP_SUMMARY
  #         echo '```bash' >> $GITHUB_STEP_SUMMARY
  #         echo "docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" >> $GITHUB_STEP_SUMMARY
  #         echo '```' >> $GITHUB_STEP_SUMMARY
  
  # create-release:
  #   needs: test
  #   runs-on: ubuntu-22.04
  #   permissions:
  #     contents: write
    
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
      
  #     - name: Extract version
  #       id: get-version
  #       run: |
  #         VERSION=$(grep -Po '(?<=^version = ")[^"]*' pyproject.toml | head -1)
  #         echo "version=$VERSION" >> $GITHUB_OUTPUT
  #         echo "Extracted version: $VERSION"
      
  #     - name: Generate release notes
  #       id: release-notes
  #       run: |
  #         echo "## 🚀 API Docker Service Release v${{ steps.get-version.outputs.version }}" > release_notes.md
  #         echo "" >> release_notes.md
  #         echo "### 📦 Docker Image" >> release_notes.md
  #         echo "- Registry: ${{ env.REGISTRY }}" >> release_notes.md
  #         echo "- Image: ${{ env.IMAGE_NAME }}" >> release_notes.md
  #         echo "- Version: ${{ steps.get-version.outputs.version }}" >> release_notes.md
  #         echo "" >> release_notes.md
  #         echo "### 🐳 Pull command:" >> release_notes.md
  #         echo '```bash' >> release_notes.md
  #         echo "docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.get-version.outputs.version }}" >> release_notes.md
  #         echo "docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" >> release_notes.md
  #         echo '```' >> release_notes.md
  #         echo "" >> release_notes.md
          
  #         # Add commit history since last tag if there are previous tags
  #         if git describe --tags --abbrev=0 &> /dev/null; then
  #           LAST_TAG=$(git describe --tags --abbrev=0)
  #           echo "### 📝 Changes since $LAST_TAG" >> release_notes.md
  #           git log --pretty=format:"* %s" $LAST_TAG..HEAD >> release_notes.md
  #         else
  #           echo "### 📝 Initial Release" >> release_notes.md
  #           git log --pretty=format:"* %s" >> release_notes.md
  #         fi
      
  #     - name: Create GitHub Release
  #       uses: softprops/action-gh-release@v1
  #       with:
  #         name: "Release v${{ steps.get-version.outputs.version }}"
  #         tag_name: "v${{ steps.get-version.outputs.version }}"
  #         body_path: release_notes.md
  #         draft: false
  #         prerelease: false
  #         token: ${{ secrets.GITHUB_TOKEN }}
